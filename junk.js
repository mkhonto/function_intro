var multiplyPara = function(pOne, pTwo){
  var result = pOne * pTwo;
  return result
};

var hello = multiplyPara(3, 5); // to call the mulptiplePara function
console.log(hello);

var world = multiplyPara(7, 9); // a new function
console.log(world);
